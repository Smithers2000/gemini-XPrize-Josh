#secrets and APi Keys
.env
.env.local

#Node / Dependency directories (just in case you use them later)
node_modules/

# OS generated Files
.DS_store
Thumbs.db

#we want it to ignore the private firebase api key
*.json
#instructions from firebase:
#  import firebase_admin
#  from firebase_admin import credentials

#  cred = credentials.Certificate("path/to/serviceAccountKey.json")
#  firebase_admin.initialize_app(cred)

#we also need it to not commit the byte code that we generate when we run the code, so we will add the following to the .gitignore file:
__pycache__/
*.pyc
